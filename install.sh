#!/bin/bash
python3 -m venv ./venv # install virtual environment named "venv" in the current dir
# Then we can activate the venv by "source venv/bin/activate"
# Then check if python used is actually from the venv by "which python"
# install libraries by "pip install -r requirements.txt" 
